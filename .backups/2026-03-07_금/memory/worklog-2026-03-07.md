# 작업 기록 — 2026-03-07 (24시간)

> 커밋 수: 약 17개 | 수정 파일: 20+ | 빌드 상태: ✅ 0 에러

---

## 1. 관리자 비밀번호 보안 강화

**목적**: 클로드 웹에 평문 비번이 노출되어 암호화 요청

### 작업 내용
- `scripts/set_admin_password.py` 신규 생성 — PBKDF2-SHA256 해시, getpass 불가시 입력
- `api_server.py` — `_verify_admin_password()` 추가 (평문 하위호환 + pbkdf2 검증)
- `/api/admin/change-password` POST 엔드포인트 추가
- `admin_app/bridge_admin.py` — `🔑 Change PW` 버튼 + Tkinter 마스킹 다이얼로그 (`show="●"`)

### 교훈
- `_change_password_dialog` 메서드가 `_InterviewDialog` 내부에 잘못 배치됨 → `BridgeAdminApp`으로 이동하여 수정
- 서버가 localhost:8000이 아닌 Render 프로덕션을 바라봐야 함 → `.env`에 `BRIDGE_ADMIN_API_URL=https://bridge-n7hk.onrender.com` 추가

---

## 2. EditModeBar (Admin 플로팅 바) 개선

**목적**: 기존 노란 어드민 바가 메뉴·콘텐츠를 가리는 문제

### 작업 내용
- `EditModeBar.tsx` — 상단 고정 바 → `position: fixed` 드래그 가능한 투명 붉은 박스로 전환
- 초기 위치: 우측 여백 (`window.innerWidth - 180, window.innerHeight / 2`)
- `useRef` + `mousemove/mouseup` 이벤트로 자유 드래그 구현
- 디자인: `rgba(239,68,68,0.08)` 배경, 붉은 테두리, ADMIN 라벨 + Exit 버튼

---

## 3. 게시판 순서 저장 + 드래그 UX 개선

**목적**: 파란 "순서 저장" 바가 콘텐츠를 가리고, 드래그가 작은 핸들로만 가능

### 작업 내용
- `community/[board]/page.tsx` — 상단/하단 파란 바 제거
- `BoardHeader` 컴포넌트에 `💾 순서 저장` 버튼 인라인 추가 (항시 표시)
- `SortableItem` — `DragHandleCtx` 제거, 카드 전체에 `{...listeners}` 적용 (전체 드래그)
- `ListLayout`, `CardGridLayout`, `PhotoCardsLayout` 모두 동일 적용

---

## 4. DocBlock 레이아웃 개선

### 작업 내용
- 메모 상단에 노란 박스 표시
- 편집 버튼 명확화
- 이동 버튼 텍스트 추가

---

## 5. 스프레드시트 (BridgeAdminSheet) 개선

### 두 차례 개선
1. **사진 DB 저장**: 이미지 데이터 DB에 영구 저장
2. **로딩 속도 개선**: 단일 요청 + localStorage 캐시 → 재방문 시 즉시 표시

### Employer 데이터 연동
- `jobs` 테이블에서 employer 데이터 직접 참조
- status 드롭다운 fix
- AG Grid TypeScript 타입 에러 수정

---

## 6. 어드민 버튼 색상 통일

**목적**: 관리자 페이지 전체에서 버튼 색상 일관성

| 역할 | 색상 |
|------|------|
| 수정 | 주황 |
| 저장 | 파란 |
| 취소 | 회색 |
| 삭제 | 빨강 |

- 전체 게시판(posts, jobs, testimonials, faq 등) 일괄 적용

---

## 7. MailComposer 전면 업그레이드

**파일**: `web_frontend/src/app/admin/components/MailComposer.tsx`

### 기능 추가 (1차)
| 기능 | 내용 |
|------|------|
| 다중 수신자 입력 | 콤마·세미콜론·줄바꿈으로 여러 이메일 붙여넣기 파싱 |
| 글자색 팔레트 | 10색 확장 |
| 배경색 팔레트 | 신규 — 7색 (노랑·연두·하늘·연분홍·연보라·주황·없음) |
| 줄간격 | 1.0 ~ 2.5 선택 셀렉트 |
| 구분선 | HR 삽입 버튼 |
| 링크 삽입 | 인라인 패널 (표시텍스트 + URL, Enter 삽입) |
| 이미지 본문 삽입 | 🖼 버튼으로 이미지 → 본문에 직접 표시 |
| 이미지 붙여넣기 | Ctrl+V → 클립보드 이미지 인라인 삽입 |
| 첨부파일 위치 | 제목 위로 이동, 파일아이콘+용량 표시, 대용량 지원 |
| 발송 카운트 | 선택업체 + 수동입력 이메일 합산 |

### 기능 추가 (2차 — 배경색 확장 + 이미지 리사이즈)
- 배경색 11색 확장: **옅은노랑·옅은빨강·옅은파랑·옅은보라** 추가
- 이미지 클릭 → 파란 테두리로 선택 표시
- 플로팅 툴바: 원본/25%/50%/75%/100% 프리셋 + px 직접입력 + 삭제
- 우하단 파란 핸들 드래그로 자유 리사이즈
- 스크롤 시 툴바 위치 갱신 (capture phase scroll listener)

### 버그 수정 (테스트 중 발견)
1. `onPaste` stale closure — `clipboardData.getData()` 직접 읽기로 수정
2. 이미지 툴바 스크롤 추적 누락 — `document.addEventListener('scroll', update, true)` 추가
3. BG 팔레트 너비 부족 — `w-[110px]` → `w-[165px]`

---

## 8. 보안 강화 (api_server.py)

### 2단계 강화
1. **1차**: 해외 해킹 방지 — 누진차단, 허니팟, 관리자 로그인 보호
2. **2차**: 공격 패턴 강화 감지 확대
   - 미국·영국·남아공: 1회 패턴 탐지 즉시 24h 차단
   - 중국·UAE·러시아·북한·브라질 + 추가 국가 확대

---

## 9. FAQ 드래그앤드롭 순서변경

- 번호 배지를 grab 핸들로 활용
- `dnd-kit` 기반 구현
- 순서 저장 API 연동

---

## 10. AllCandidatesGrid 통합

- 전체 후보자 그리드 컴포넌트 통합
- 관련 타입 에러 수정

---

## 환경 관련 트러블슈팅

| 문제 | 원인 | 해결 |
|------|------|------|
| dev 서버 500 에러 (mail-send) | `npm run build` 실행으로 `.next/` 덮어씌워짐 → dev 서버 청크 ID 불일치 | PID 1672 kill → `nohup npm run dev &` 재시작 |
| BridgeAdminSheet TS 에러 | `DataRow` 타입 충돌 (두 파일에 동명 타입) | stash pop으로 수정본 복원 |
| `_change_password_dialog` 위치 오류 | `_InterviewDialog` 내부에 잘못 배치됨 | `BridgeAdminApp` 클래스로 이동 |

---

## 핵심 파일 변경 목록

```
web_frontend/src/app/admin/components/MailComposer.tsx        ← 주요 업그레이드
web_frontend/src/app/admin/components/EditModeBar.tsx         ← 드래그 플로팅 바
web_frontend/src/app/community/[board]/page.tsx               ← 드래그 UX + 순서저장 인라인
web_frontend/src/app/admin/components/BridgeAdminSheet.tsx    ← 속도 개선 + Employer 연동
api_server.py                                                 ← 보안 강화, 비번변경 API
admin_app/bridge_admin.py                                     ← 비번변경 GUI
scripts/set_admin_password.py                                 ← 신규 (PBKDF2)
```

---

## 다음 작업 후보

- Interview Management 세팅 재구현 (이전 세션 미저장 작업)
- 프로덕션 배포 (Vercel + Render 동기화)
- 메일 발송 API 대용량 첨부파일 실제 테스트
