# ── Environment & Secrets ──────────────────────────────────────
.env
.env.*
!.env.example
!web_frontend/.env.production
account*.env
.env.craig

# ── Credentials & Keys ────────────────────────────────────────
*.pem
*.key
*.p12
*.pfx
.secrets.enc.json
google_service_account*.json
CREDENTIALS_REFERENCE.md

# ── Databases ─────────────────────────────────────────────────
# DB는 git에 절대 포함하지 않음 (Render는 /data/master.db 독립 운용)
*.db
*.db-shm
*.db-wal
master.db
master.db.*
*.sqlite
*.sqlite3
data/

# ── Resume Converter 임시 폴더 (PII 포함) ─────────────────────
tools/resume_converter/inbox/*/
tools/resume_converter/processing/
tools/resume_converter/output/
tools/resume_converter/originals/
tools/resume_converter/logs/
tools/resume_converter/.rc_key

# ── Legacy data / CSV dumps ───────────────────────────────────
*.csv
*.dump
*.sql
legacy_posts.*
original_candidates/
original_jobs/
intake/
unified_bridge_data.xlsx

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
venv/
.venv/
*.log

# ── Node / Next.js ────────────────────────────────────────────
node_modules/
web_frontend/node_modules/
web_frontend/.next/
web_frontend/out/
.next/

# ── IDE & OS ──────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
Desktop.ini
.DS_Store

# ── Screenshots & images (RPA output) ─────────────────────────
screenshots/
images/

# ── Temp / scratch files ──────────────────────────────────
openapi_tmp.json
*.tmp.json

# ── Logs ──────────────────────────────────────────────────────
logs/
*.log

# ── Obsidian docs (local only) ────────────────────────────────
obsidian_docs/

# ── Sheets state (local cache) ────────────────────────────────
sheets_state.json

# ── Backups (local only) ─────────────────────────────────────
backups/
bridge-overnight/

# ── Security Keys ────────────────────────────────────────────
.bridge.key
.bx/
vault/data/
mail_log_*.json
.vault.enc.json

# ── Archive (large test data, old files) ─────────────────────
archive/
tmp_photos/

# ── Local workspace (personal, not shared) ───────────────────
.gsd/
context.md

# ── Misc ──────────────────────────────────────────────────────
NUL
*.bak
*.tmp
debug_*.html
night_shift_test.log
*.wav
*.xlsx

# ── PyInstaller build artifacts ──────────────────────────────
bridge_agent/build_temp/
bridge_agent/dist/
*.pkg
*.spec
bridge_agent/*.spec
bridge_agent/*.ico
!original_candidates/bridge-admin-sheet.jsx
web_frontend/tsconfig.tsbuildinfo
.env.scraper
build_log*.txt

# ── MasterVault & Watchdog (PHASE 7 추가 — 절대 커밋 금지) ────────────────
.vault/
*.enc
master.salt
active_keys.enc
key_rotation.jsonl
ddns_last_ip.txt
ddns_watchdog.log
bridge_security.log
audit.jsonl
security_log.jsonl
bridge_totp_qr.png
setup_totp.py
init_vault_env.py

# ── Network Security & Device Registry (세션 21 추가) ─────────────
.device_registry.json
.trusted_ips.json
.device_key

# ── Security Hardening (세션 21 추가) ────────────────────────────
security_config/
!security_config/SECURITY_GUIDE.md
security_config/keys/
security_config/wireguard/
security_config/render.env
security_config/firewall_rules.json
.env
.rpa_vault.json
