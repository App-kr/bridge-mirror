# 세션 로그 — bridge-base
**날짜/시간**: 2026-03-12 12:03
**프로젝트**: Q:\Claudework\bridge base
**브랜치**: main

---

## Git 최근 커밋 (최근 5개)
```
9edf96c fix: _ov_launch schtasks 방식으로 완전 교체 — svchost 자식, Job Object 100% 탈출 확인
d634043 fix: 오버레이 조기 종료 원인 수정 — 스테일 state/레이스 컨디션 이중 방어
74e44c2 fix: ShellExecuteW→Shell.Application COM 교체 — Explorer 프로세스에서 직접 생성, Job Object 완전 탈출
722e502 fix: Popen→ShellExecuteW 교체 — Explorer 자식으로 CMD 연쇄종료 완전 차단
da47c37 fix: CREATE_BREAKAWAY_FROM_JOB 추가 + daemon=False — CMD Job Object 완전 탈출
```

## Git 변경 파일 요약
```
 .claude/settings.local.json                  |  12 +-
 docs/obsidian/BRIDGE_작업일지.md              |   6 +
 tools/backup_state.json                       | 116 +-
 81 files changed, 131 insertions(+), 19630 deletions(-)
```
주요 변경: 이전 백업 폴더 2개 삭제(D), 작업일지 갱신, 신규 파일 다수(skills, docs)

---

## 이번 세션 작업 요약

### 1. excalidraw-diagram 스킬 설치
- GitHub에서 클론: `coleam00/excalidraw-diagram-skill`
- 경로: `.claude/skills/excalidraw-diagram/`
- playwright + chromium 설치 완료 (python3 기반)

### 2. BRIDGE 시스템 아키텍처 다이어그램 생성
- 파일: `docs/bridge-architecture.excalidraw`
- PNG 렌더링: `docs/bridge-architecture.png`
- 구성 요소:
  - 좌측 — 프로덕션 시스템: 구직자/기업HR → Vercel(Next.js) → Render(FastAPI) → SQLite(master.db)
  - 우측 — 7-에이전트 팀: Director → Server Eng / Security / Frontend / QA / PM Agent / Content
  - 보안 벨트: JWT · HMAC · Rate Limit · OWASP Top 10
- 2회 렌더링 반복(수직 정렬 개선) 후 최종 확정

### 3. PNG 뷰어로 다이어그램 열기
- `start "" "Q:\Claudework\bridge base\docs\bridge-architecture.png"` 실행

---

## 발견된 이슈

| 항목 | 상태 |
|------|------|
| `bridge_backup.py` UnicodeEncodeError (cp949) | ⚠️ 미수정 — emoji 출력 시 Windows 인코딩 충돌 |
| `git diff --stat` 에 대용량 백업 삭제 파일 다수 | ℹ️ 정상 — 이전 백업 폴더 정리 결과 |
| `.claude/skills/` 미추적 파일 다수 | ℹ️ 정상 — 신규 설치 스킬들 (gitignore 추가 검토) |

---

## 다음에 해야 할 작업

1. **`bridge_backup.py` 인코딩 수정** — `python -X utf8` 또는 `sys.stdout` 강제 UTF-8 설정
2. **`.gitignore` 갱신** — `.claude/skills/` 폴더 추가 여부 결정
3. **다이어그램 확장** — Deployment Pipeline (git → Render CI → Vercel) 흐름도 추가
4. **Excalidraw 편집** — excalidraw.com에서 드래그앤드롭으로 대화형 편집 가능
5. **대용량 백업 파일 git 정리** — `git add -A && git commit` 로 삭제 파일 커밋

---

## 파일 경로 참조
```
Q:\Claudework\bridge base\
├── docs\
│   ├── bridge-architecture.excalidraw   ← 편집 가능
│   └── bridge-architecture.png          ← 렌더링 결과
├── .claude\skills\
│   └── excalidraw-diagram\              ← 신규 설치 스킬
└── tools\
    └── bridge_backup.py                 ← 인코딩 수정 필요
```
